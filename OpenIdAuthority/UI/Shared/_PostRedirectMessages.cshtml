@{ 
    var messageData = (string)TempData["PostRedirect.Messages"];
    if(messageData != null)
    {
        var messages = messageData.Split('|');
        <div class="messages">
            @foreach(var message in messages)
            {
                <div class="message">@message</div>
            }
        </div>
    }
}
        